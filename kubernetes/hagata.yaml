apiVersion: apps/v1
kind: Deployment
metadata:
  name: wincalx-web-hagata
spec:
  selector:
    matchLabels:
      app: wincalx-web-hagata
      tier: frontend
      company: hagata-inn
  replicas: 1
  template:
    metadata:
      labels:
        app: wincalx-web-hagata
        tier: frontend
        company: hagata-inn
    spec:
      containers:
        - name: wincalx-web-hagata
          image: REPLACED_WITH_DOCKER_IMAGE_NAME
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
          ports:
            - containerPort: 80
---
kind: Service
apiVersion: v1
metadata:
  name: wincalx-web-hagata
spec:
  type: NodePort
  selector:
    app: wincalx-web-hagata
    tier: frontend
    company: hagata-inn
  ports:
    - name: http
      port: 8080
      targetPort: 80